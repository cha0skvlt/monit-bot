BOT_TOKEN=your_bot_token
CHAT_ID=your_chat_id
# Optional paths used by the bot (defaults work for Docker)
#DB_FILE=/app/db.sqlite
# If DB_FILE is a directory or a path without extension, db.sqlite will be created inside it
#LOG_FILE=/app/logs/monitor.log
#REQUEST_TIMEOUT=10

